{
  "description": "Checks whether native text-selection handling is enabled.",
  "params": {},
  "response": {
    "description": "The native text-selection feature state.",
    "properties": {
      "enabled": {
        "description": "Whether the frame should handle text selection.",
        "type": "boolean"
      }
    },
    "required": ["enabled"],
    "type": "object"
  }
}
