{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "IndexedDBStateChanged",
  "required": ["isActive"],
  "properties": {
    "isActive": {
      "type": "boolean",
      "description": "True when at least one IndexedDB connection is open, false when all connections are closed."
    }
  },
  "additionalProperties": false
}
