{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "PasskeyUsedParams",
  "description": "Notifies the native layer that a WebAuthn passkey was successfully used (created or retrieved), for pixelling purposes only.",
  "additionalProperties": false,
  "required": ["type"],
  "properties": {
    "type": {
      "description": "Which CredentialsContainer method completed the passkey ceremony.",
      "type": "string",
      "enum": ["get", "create"]
    }
  }
}
