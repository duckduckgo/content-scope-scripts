{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "PasskeyUsedParams",
  "description": "Contract for the webCompat.passkeyUsed notification sent to Android when a WebAuthn passkey ceremony succeeds, for pixelling purposes only.",
  "additionalProperties": false,
  "required": ["type"],
  "properties": {
    "type": {
      "description": "Which CredentialsContainer method initiated the passkey ceremony.",
      "type": "string",
      "enum": ["get", "create"]
    }
  }
}
