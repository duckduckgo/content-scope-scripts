{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Omnibar Config",
  "type": "object",
  "required": [
    "mode"
  ],
  "$defs": {
    "AttachmentLimits": {
      "type": "object",
      "description": "Limits the omnibar applies to image and file attachments. When omitted, the omnibar uses its built-in defaults.",
      "required": ["files", "images"],
      "properties": {
        "files": {
          "description": "Limits for file attachments (e.g. PDFs).",
          "type": "object",
          "required": ["maxPerConversation", "maxFileSizeMB", "maxTotalFileSizeBytes", "maxPagesPerFile"],
          "properties": {
            "maxPerConversation": {
              "description": "Maximum number of file attachments allowed.",
              "type": "integer"
            },
            "maxFileSizeMB": {
              "description": "Maximum size of a single file attachment, in megabytes.",
              "type": "integer"
            },
            "maxTotalFileSizeBytes": {
              "description": "Maximum combined size of all file attachments, in bytes.",
              "type": "integer"
            },
            "maxPagesPerFile": {
              "description": "Maximum number of pages allowed in a single file (e.g. PDF). Not enforced web-side.",
              "type": "integer"
            }
          }
        },
        "images": {
          "description": "Limits for image attachments.",
          "type": "object",
          "required": ["maxPerTurn", "maxPerConversation", "maxInputCharsWithAttachments"],
          "properties": {
            "maxPerTurn": {
              "description": "Maximum number of images allowed in a single submission.",
              "type": "integer"
            },
            "maxPerConversation": {
              "description": "Maximum number of images allowed across an entire conversation.",
              "type": "integer"
            },
            "maxInputCharsWithAttachments": {
              "description": "Maximum prompt length (characters) when attachments are present. Not enforced web-side.",
              "type": "integer"
            }
          }
        }
      }
    },
    "UpsellType": {
      "type": "string",
      "description": "Which upsell flow a gated item leads to: 'subscribe' (non-subscribers, shown as 'Try for free') or 'upgrade' (existing subscribers who need a higher tier, shown as 'Upgrade').",
      "enum": ["subscribe", "upgrade"]
    },
    "AIModelSection": {
      "type": "object",
      "description": "A section of AI models with an optional header and a list of model items.",
      "required": ["items"],
      "properties": {
        "header": {
          "description": "Optional section header text (e.g. 'Advanced Models - DuckDuckGo subscription')",
          "type": "string"
        },
        "items": {
          "description": "List of AI models in this section",
          "type": "array",
          "items": { "$ref": "#/$defs/AIModelItem" }
        }
      }
    },
    "ReasoningEffortOption": {
      "type": "object",
      "description": "A reasoning-effort option for a reasoning-capable model, including its localized display copy and availability.",
      "required": ["id", "name", "isAvailable"],
      "properties": {
        "id": {
          "description": "Stable server key for this reasoning-effort option; round-tripped on submit.",
          "$ref": "./reasoning-effort.json"
        },
        "name": {
          "description": "Localized display name for this reasoning-effort option.",
          "type": "string"
        },
        "description": {
          "description": "Optional localized subtitle shown beneath the name.",
          "type": "string"
        },
        "isAvailable": {
          "description": "Whether this option is selectable (true) or gated behind a subscription upsell (false).",
          "type": "boolean"
        },
        "upsell": {
          "description": "For a gated (isAvailable: false) option, which upsell flow it leads to. Absent for available options.",
          "$ref": "#/$defs/UpsellType"
        }
      }
    },
    "AIModelItem": {
      "type": "object",
      "description": "An individual AI model available for selection.",
      "required": ["id", "name", "shortName", "isAvailable", "supportsImageUpload"],
      "properties": {
        "id": {
          "description": "Model identifier",
          "type": "string"
        },
        "name": {
          "description": "Full display name",
          "type": "string"
        },
        "shortName": {
          "description": "Short display name for the model selector button",
          "type": "string"
        },
        "description": {
          "description": "Optional short description shown beneath the model name in the selector.",
          "type": "string"
        },
        "isAvailable": {
          "description": "Whether the model is available and selectable",
          "type": "boolean"
        },
        "accessTier": {
          "description": "Access tier that grants this model (e.g. 'internal', 'free', 'plus', 'pro'). Drives the tier badge (Plus/Pro/Internal) in the model selector.",
          "type": "string",
          "enum": ["internal", "free", "plus", "pro"]
        },
        "supportsImageUpload": {
          "description": "Whether this model supports image attachments",
          "type": "boolean"
        },
        "supportedFileTypes": {
          "description": "MIME types this model accepts as file attachments; empty or omitted means it accepts no files. Drives the file picker's `accept` attribute and clears attached files the newly-selected model doesn't support.",
          "type": "array",
          "items": { "type": "string" }
        },
        "supportedTools": {
          "description": "Tools this model supports.",
          "type": "array",
          "items": { "$ref": "./tool-id.json" }
        },
        "reasoningEfforts": {
          "description": "Reasoning-effort options this model supports, each with localized copy and availability. Empty or omitted means the reasoning picker is hidden for this model.",
          "type": "array",
          "items": { "$ref": "#/$defs/ReasoningEffortOption" }
        },
        "upsell": {
          "description": "For a gated (disabled) model, which upsell flow it leads to. Absent for enabled models.",
          "$ref": "#/$defs/UpsellType"
        }
      }
    }
  },
  "properties": {
    "mode": {
      "title": "Omnibar Mode",
      "type": "string",
      "enum": ["search", "ai"]
    },
    "enableAi": {
      "title": "Enable Duck.ai",
      "type": "boolean",
      "default": true
    },
    "showAiSetting": {
      "title": "Show Duck.ai Setting",
      "type": "boolean",
      "default": true
    },
    "showCustomizePopover": {
      "title": "Show Customize Popover",
      "description": "Controls a popover that onboards users and points them towards how to disable the feature via the customizer",
      "type": "boolean",
      "default": false
    },
    "enableRecentAiChats": {
      "title": "Enable Recent AI Chats",
      "type": "boolean",
      "default": false
    },
    "enableAiChatTools": {
      "title": "Enable AI Chat Tools",
      "description": "Feature flag to enable AI chat tools (model selector, image attachments)",
      "type": "boolean",
      "default": false
    },
    "selectedModelId": {
      "title": "Selected Model ID",
      "description": "The user's currently selected AI model, persisted across tabs",
      "type": "string"
    },
    "selectedReasoningEffort": {
      "$ref": "./reasoning-effort.json"
    },
    "aiModelSections": {
      "title": "AI Model Sections",
      "description": "Sections of AI models for the model selector.",
      "type": "array",
      "items": { "$ref": "#/$defs/AIModelSection" }
    },
    "attachmentLimits": {
      "title": "Attachment Limits",
      "$ref": "#/$defs/AttachmentLimits"
    },
    "showViewAllAiChats": {
      "title": "Show View All AI Chats",
      "description": "When enabled, shows a 'View all chats' link at the bottom of the recent AI chats list",
      "type": "boolean",
      "default": false
    },
    "enableImageGeneration": {
      "title": "Enable Image Generation",
      "description": "Show 'Create Image' toggle in the AI chat toolbar.",
      "type": "boolean",
      "default": false
    },
    "enableWebSearch": {
      "title": "Enable Web Search",
      "description": "Allow AI chat submissions to include web search tool.",
      "type": "boolean",
      "default": false
    },
    "enableVoiceChatAccess": {
      "title": "Enable Voice Chat Access",
      "description": "Show a 1-click voice-chat button in place of the AI chat submit button when the input is empty.",
      "type": "boolean",
      "default": false
    },
    "enableCustomizeResponses": {
      "title": "Enable Customize Responses",
      "description": "Show the 'Customize responses' entry in the AI chat Tools menu. Selecting it opens the Customize Responses modal.",
      "type": "boolean",
      "default": false
    },
    "customizeSubLabel": {
      "title": "Customize Responses Sub-label",
      "description": "Summary of the user's current response customization (e.g. 'Professional, Concise'), shown as the description under the 'Customize responses' Tools-menu row. Omitted when responses haven't been customized, in which case the default description is shown.",
      "type": "string"
    },
    "hasCustomization": {
      "title": "Has Customization",
      "description": "True once the user has customized their responses. Gates the on/off toggle shown alongside the 'Customize responses' Tools-menu row.",
      "type": "boolean",
      "default": false
    },
    "customizationActive": {
      "title": "Customization Active",
      "description": "Whether the stored customization is currently applied. Drives the checked state of the toggle in the 'Customize responses' Tools-menu row.",
      "type": "boolean",
      "default": false
    },
    "enableAskAiSuggestion": {
      "title": "Enable Ask Duck.ai Suggestion",
      "description": "Controls whether the inline 'Ask Duck.ai: <query>' suggestion is rendered in the omnibar dropdown. Missing/undefined is treated as true for backward compatibility. Does not affect the Duck.ai mode pill or any other AI affordance — those remain governed by enableAi.",
      "type": "boolean",
      "default": true
    },
    "enableAttachTabs": {
      "title": "Enable Attach Tabs",
      "description": "Show the paperclip entry point and accept `@` mentions for attaching open tabs as context to a Duck.ai submission. When true, the omnibar can call `omnibar_getOpenTabs` and `omnibar_getTabContent`.",
      "type": "boolean",
      "default": false
    },
    "enableAiChatDeletion": {
      "title": "Enable AI Chat Deletion",
      "description": "Show a delete button on recent AI chat suggestions. When true, clicking the button prompts a native confirmation dialog before deleting the chat.",
      "type": "boolean",
      "default": false
    },
    "enableSearchSuggestionDeletion": {
      "title": "Enable Search Suggestion Deletion",
      "description": "Show a delete button on history entry suggestions. When true, clicking the button removes the entry from browsing history.",
      "type": "boolean",
      "default": false
    }
  }
}
