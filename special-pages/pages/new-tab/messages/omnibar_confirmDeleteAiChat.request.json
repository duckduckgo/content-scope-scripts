{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Confirm Delete AI Chat Params",
  "description": "Requests a native confirmation dialog for deleting an AI chat. Native shows the dialog and responds with the user's choice.",
  "type": "object",
  "required": ["chatId", "title"],
  "properties": {
    "chatId": {
      "description": "Unique identifier of the chat to delete",
      "type": "string"
    },
    "title": {
      "description": "The chat title, displayed in the native confirmation dialog",
      "type": "string"
    }
  }
}
