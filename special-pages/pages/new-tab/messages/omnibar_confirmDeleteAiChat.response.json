{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Confirm Delete AI Chat Response",
  "description": "Response from native after the user interacts with the confirmation dialog.",
  "type": "object",
  "required": ["action"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["delete", "none"]
    }
  }
}
