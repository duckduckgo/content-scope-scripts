{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Confirm Delete All AI Chats Response",
  "description": "Response from native after the user interacts with the confirmation dialog. On 'delete' the chats are already gone, so the page should re-fetch.",
  "type": "object",
  "required": ["action"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["delete", "none"]
    }
  }
}
