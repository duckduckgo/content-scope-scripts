{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NTP TelemetryEvent",
  "type": "object",
  "required": ["attributes"],
  "properties": {
    "attributes": {
      "oneOf": [
        {
          "type": "object",
          "title": "Stats Show More",
          "required": ["name", "value"],
          "properties": {
            "name": {
              "const": "stats_toggle"
            },
            "value": {
              "type": "string",
              "enum": ["show_more", "show_less"]
            }
          }
        },
        {
          "type": "object",
          "title": "Example Telemetry Event",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "ntp_example"
            }
          }
        },
        {
          "type": "object",
          "title": "Customizer Drawer State",
          "required": ["name", "value"],
          "properties": {
            "name": {
              "const": "customizer_drawer"
            },
            "value": {
              "type": "object",
              "required": ["state"],
              "properties": {
                "state": {
                  "type": "string",
                  "enum": ["opened", "closed"]
                },
                "themeVariantPopoverWasOpen": {
                  "type": "boolean",
                  "description": "True if the theme variant popover was visible when the drawer was opened"
                }
              }
            }
          }
        },
        {
          "type": "object",
          "title": "Chat Delete Button Clicked",
          "description": "Fired when the user clicks the delete button on a chat suggestion, before the native dialog opens.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "ntp_aichat_recent_chat_delete_button_clicked"
            }
          }
        },
        {
          "type": "object",
          "title": "Chat Delete Confirmed",
          "description": "Fired after the user confirms deletion in the native dialog.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "ntp_aichat_recent_chat_delete_confirmed"
            }
          }
        },
        {
          "type": "object",
          "title": "Chat Delete Cancelled",
          "description": "Fired after the user cancels the native deletion dialog.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "ntp_aichat_recent_chat_delete_cancelled"
            }
          }
        },
        {
          "type": "object",
          "title": "Search Suggestion Deleted",
          "description": "Fired when the user clicks the delete button on a history entry suggestion.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "ntp_autocomplete_result_deleted"
            }
          }
        }
      ]
    }
  }
}
