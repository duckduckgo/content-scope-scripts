{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NTP TelemetryEvent",
  "type": "object",
  "required": ["attributes"],
  "properties": {
    "attributes": {
      "oneOf": [
        {
          "type": "object",
          "title": "Stats Show More",
          "required": ["name", "value"],
          "properties": {
            "name": {
              "const": "stats_toggle"
            },
            "value": {
              "type": "string",
              "enum": ["show_more", "show_less"]
            }
          }
        },
        {
          "type": "object",
          "title": "Example Telemetry Event",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "ntp_example"
            }
          }
        },
        {
          "type": "object",
          "title": "Customizer Drawer State",
          "required": ["name", "value"],
          "properties": {
            "name": {
              "const": "customizer_drawer"
            },
            "value": {
              "type": "object",
              "required": ["state"],
              "properties": {
                "state": {
                  "type": "string",
                  "enum": ["opened", "closed"]
                },
                "themeVariantPopoverWasOpen": {
                  "type": "boolean",
                  "description": "True if the theme variant popover was visible when the drawer was opened"
                }
              }
            }
          }
        },
        {
          "type": "object",
          "title": "Omnibar Model Picker Shown",
          "description": "Fired once when the user opens the omnibar model picker. This is a picker impression; it does not imply that the user activated a model or an upsell.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "omnibar_model_picker_shown"
            }
          }
        },
        {
          "type": "object",
          "title": "Omnibar Model Picker Try For Free Shown",
          "description": "Fired when the user activates a gated model row whose displayed CTA is 'Try for free', immediately before the web UI requests the native subscription upsell. Despite the historical '_shown' suffix, this is an activation event, not an impression.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "omnibar_model_picker_tryforfree_shown"
            }
          }
        },
        {
          "type": "object",
          "title": "Omnibar Model Picker Upgrade Shown",
          "description": "Fired when the user activates a gated model row whose resolved upsell presentation is 'Upgrade'. This activation bucket is derived from the item's upsell type and free-trial eligibility; the following native route is still determined by the item's upsell value, so this event can precede a subscription-upsell request. Despite the historical '_shown' suffix, this is an activation event, not an impression.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "omnibar_model_picker_upgrade_shown"
            }
          }
        },
        {
          "type": "object",
          "title": "Omnibar Reasoning Picker Shown",
          "description": "Fired once when the user opens the omnibar reasoning picker. This is a picker impression; it does not imply that the user activated a reasoning effort or an upsell.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "omnibar_reasoning_picker_shown"
            }
          }
        },
        {
          "type": "object",
          "title": "Omnibar Reasoning Picker Try For Free Shown",
          "description": "Fired when the user activates a gated reasoning-effort row whose displayed CTA is 'Try for free', immediately before the web UI requests the native subscription upsell. Despite the historical '_shown' suffix, this is an activation event, not an impression.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "omnibar_reasoning_picker_tryforfree_shown"
            }
          }
        },
        {
          "type": "object",
          "title": "Omnibar Reasoning Picker Upgrade Shown",
          "description": "Fired when the user activates a gated reasoning-effort row whose resolved upsell presentation is 'Upgrade'. This activation bucket is derived from the item's upsell type and free-trial eligibility; the following native route is still determined by the item's upsell value, so this event can precede a subscription-upsell request. Despite the historical '_shown' suffix, this is an activation event, not an impression.",
          "required": ["name"],
          "properties": {
            "name": {
              "const": "omnibar_reasoning_picker_upgrade_shown"
            }
          }
        }
      ]
    }
  }
}
