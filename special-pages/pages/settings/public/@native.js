/**
 * @param {{state: Record<string, any>, structure: SettingsStructure}} args
 * @param initialSetup
 */
export async function process(args, initialSetup) {
    // args.structure.excludedElements.push('vpn.location.selector');
    // args.structure.excludedElements.push('vpn.location.nearestLocation');
    return args;
}
