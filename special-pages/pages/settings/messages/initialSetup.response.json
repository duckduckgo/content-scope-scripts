{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["locale", "env", "platform", "settingsData"],
  "properties": {
    "locale": {
      "type": "string"
    },
    "env": {
      "type": "string",
      "enum": ["development", "production"]
    },
    "platform": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "enum": ["macos", "windows", "android", "ios", "integration"]
        }
      }
    },
    "settingsData": {"$ref": "./types/settings-data.json"},
    "defaultStyles": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "$ref": "types/default-styles.json"
        }
      ]
    }
  }
}
