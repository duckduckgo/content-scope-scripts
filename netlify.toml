[build]
command = "npm ci && npm run docs-netlify"
publish = "docs"
ignore = "git log -1 --pretty=%B | grep dependabot"

# Skip Netlify dependency install (avoids Swift package build)
[build.environment]
NETLIFY_SKIP_INSTALL = "true"