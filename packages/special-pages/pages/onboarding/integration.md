## Integration

- Serve the entire folder found under `build/<PLATFORM>/pages/onboarding`
- Ensure that assets can be resolved, for example, `js/index.js` should resolve to `build/<PLATFORM>/pages/onboarding/js/index.js`
- Implement [message handlers](/classes/Onboarding_Page.OnboardingMessages.html)
